interface:
  display_name: "CR All"
  short_description: "Run full-chain reviews and optional finding-driven fixes for feature workflows."
  default_prompt: "Use cr-all to perform a full-chain expert review of this feature workflow, then fix the proven findings if requested."
